[[plugins]]
  package = "netlify-plugin-flutter"

[build]
  base = "frontend"
  command = "flutter build web --release --dart-define=API_BASE_URL=https://1-mgt1.onrender.com"
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "stable"
